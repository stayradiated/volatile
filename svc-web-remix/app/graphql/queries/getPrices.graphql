query getPrices {
  kc_stripe_product(where: { active: { _eq: true } }) {
    id
    active
    name
    stripe_prices(where: { active: { _eq: true } }) {
      id
      type
      recurring_interval
      recurring_interval_count
      unit_amount
      currency
    }
  }
}
