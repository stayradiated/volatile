query getSubscriptions($status: String) {
  query_subscriptions(status: $status) {
    subscriptions {
      id
      status
      created
      current_period_end
      plan {
        interval
        interval_count
        amount
        currency
      }
      latest_invoice{
        payment_intent{
          client_secret
        }
      }
    }
  }
}
