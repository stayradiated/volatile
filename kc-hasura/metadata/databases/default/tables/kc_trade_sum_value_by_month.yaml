table:
  name: trade_sum_value_by_month
  schema: kc
select_permissions:
- permission:
    columns:
    - user_uid
    - month
    - primary_currency
    - secondary_currency
    - sum
    filter:
      user_uid:
        _eq: X-Hasura-User-Id
    limit: 50
  role: user
