table:
  name: trade_sum_total_value_by_week
  schema: kc
select_permissions:
- permission:
    columns:
    - user_uid
    - week
    - primary_currency
    - secondary_currency
    - sum
    filter:
      user_uid:
        _eq: X-Hasura-User-Id
    limit: 100
  role: user
