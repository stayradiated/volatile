table:
  name: trade_sum_total_value_by_month
  schema: kc
select_permissions:
- permission:
    columns:
    - user_uid
    - month
    - primary_currency
    - secondary_currency
    - sum
    filter:
      user_uid:
        _eq: X-Hasura-User-Id
    limit: 100
  role: user
