table:
  name: stripe_product
  schema: kc
array_relationships:
  - name: stripe_prices
    using:
      foreign_key_constraint_on:
        column: product_id
        table:
          name: stripe_price
          schema: kc
select_permissions:
  - role: user
    permission:
      columns:
        - active
        - description
        - id
        - name
      filter: {}
