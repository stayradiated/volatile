table:
  name: currency
  schema: kc
select_permissions:
  - role: guest
    permission:
      columns:
        - name
        - symbol
      filter: {}
  - role: user
    permission:
      columns:
        - symbol
        - created_at
        - updated_at
        - name
      filter: {}
