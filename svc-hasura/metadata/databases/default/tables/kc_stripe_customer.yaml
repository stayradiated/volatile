table:
  name: stripe_customer
  schema: kc
array_relationships:
  - name: stripe_subscriptions
    using:
      foreign_key_constraint_on:
        column: customer_id
        table:
          name: stripe_subscription
          schema: kc
